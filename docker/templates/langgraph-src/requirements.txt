# LangGraph 运行时依赖
# langgraph-cli[inmem] 会自动安装兼容的 langgraph / langgraph-api
langgraph-cli[inmem]==0.4.29
langgraph-checkpoint-sqlite==3.1.0

# LLM 调用（OpenAI 兼容接口，可覆盖 OpenAI / Moonshot / 自定义接口）
openai==1.75.0

# 常用工具依赖
pillow==12.2.0
piexif==1.1.3

# HTTP 客户端（调用 OpenCode HTTP Server API）
requests>=2.32.0

# 其他可能用到的依赖
python-dateutil==2.9.0
